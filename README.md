YouTube MP3 downloader.

Create your virtual environment and install yt-dlp.

pip install -r requirements.txt

To run : python yt2mp3.py

Then, you can download any music you want from YouTube.
Paste the link and press enter - if there are multiple links, add space between them.
